---
name: cli-cost-usage
description: View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.

## Quick install

```bash
npm install -g devilroute   # or: npx devilroute
devilroute --version
```

## Subcommands

### `cost`

**Flags:**

- `--period <range>`
- `--since <date>`
- `--until <date>`
- `--group-by <field>`
- `--api-key <key>`
- `--limit <n>`

**Example:**

```bash
devilroute cost
```

### `pricing`

**Example:**

```bash
devilroute pricing
```

### `pricing sync`

**Flags:**

- `--provider <p>`
- `--force`

**Example:**

```bash
devilroute pricing sync
```

### `pricing list`

**Flags:**

- `--provider <p>`
- `--model <m>`
- `--limit <n>`

**Example:**

```bash
devilroute pricing list
```

### `pricing get <model>`

**Example:**

```bash
devilroute pricing get <model>
```

### `pricing defaults`

**Example:**

```bash
devilroute pricing defaults
```

### `pricing show`

**Example:**

```bash
devilroute pricing show
```

### `pricing set`

**Flags:**

- `--input <p>`
- `--output <p>`
- `--cache-read <p>`
- `--cache-write <p>`

**Example:**

```bash
devilroute pricing set
```

### `pricing diff`

**Flags:**

- `--model <m>`

**Example:**

```bash
devilroute pricing diff
```

### `usage`

**Example:**

```bash
devilroute usage
```

### `usage analytics`

**Flags:**

- `--period <range>`
- `--provider <id>`

**Example:**

```bash
devilroute usage analytics
```

### `usage budget`

**Example:**

```bash
devilroute usage budget
```

### `usage list`

**Example:**

```bash
devilroute usage list
```

### `usage get [scope]`

**Example:**

```bash
devilroute usage get [scope]
```

### `usage set <amount>`

**Flags:**

- `--scope <s>`
- `--period <p>`

**Example:**

```bash
devilroute usage set <amount>
```

### `usage reset [scope]`

**Example:**

```bash
devilroute usage reset [scope]
```

### `usage quota`

**Flags:**

- `--provider <id>`
- `--check`

**Example:**

```bash
devilroute usage quota
```

### `usage logs`

**Flags:**

- `--limit <n>`
- `--search <q>`
- `--since <ts>`
- `--follow`
- `--api-key <k>`

**Example:**

```bash
devilroute usage logs
```

### `usage utilization`

**Flags:**

- `--api-key <k>`

**Example:**

```bash
devilroute usage utilization
```

### `usage history`

**Flags:**

- `--limit <n>`

**Example:**

```bash
devilroute usage history
```

### `usage proxy-logs`

**Flags:**

- `--limit <n>`

**Example:**

```bash
devilroute usage proxy-logs
```
