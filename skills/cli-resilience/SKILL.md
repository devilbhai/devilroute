---
name: cli-resilience
description: Inspect and manage circuit-breaker states, connection cooldowns, quota limits, and backoff levels from the CLI. Reset stuck providers and configure resilience thresholds.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect and manage circuit-breaker states, connection cooldowns, quota limits, and backoff levels from the CLI. Reset stuck providers and configure resilience thresholds.

## Quick install

```bash
npm install -g devilroute   # or: npx devilroute
devilroute --version
```

## Subcommands

### `quota`

**Flags:**

- `--provider <id>`
- `--json`

**Example:**

```bash
devilroute quota
```

### `resilience`

**Example:**

```bash
devilroute resilience
```

### `resilience status`

**Flags:**

- `--provider <p>`

**Example:**

```bash
devilroute resilience status
```

### `resilience breakers`

**Flags:**

- `--provider <p>`

**Example:**

```bash
devilroute resilience breakers
```

### `resilience cooldowns`

**Flags:**

- `--provider <p>`
- `--connection-id <id>`

**Example:**

```bash
devilroute resilience cooldowns
```

### `resilience lockouts`

**Flags:**

- `--provider <p>`
- `--model <m>`

**Example:**

```bash
devilroute resilience lockouts
```

### `resilience reset`

**Flags:**

- `--connection-id <id>`
- `--model <m>`
- `--all-cooldowns`
- `--yes`

**Example:**

```bash
devilroute resilience reset
```

### `resilience profile`

**Example:**

```bash
devilroute resilience profile
```

### `resilience show`

**Example:**

```bash
devilroute resilience show
```

### `resilience set`

**Example:**

```bash
devilroute resilience set
```

### `resilience config`

**Example:**

```bash
devilroute resilience config
```

### `resilience show`

**Example:**

```bash
devilroute resilience show
```

### `resilience set`

**Flags:**

- `--threshold <n>`
- `--reset-timeout <ms>`
- `--base-cooldown <ms>`

**Example:**

```bash
devilroute resilience set
```
