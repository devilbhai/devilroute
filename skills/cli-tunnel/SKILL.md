---
name: cli-tunnel
description: Start and stop tunnel connections (ngrok, Cloudflare, custom) from the CLI. Inspect active tunnel URLs, configure authentication, and test external reachability.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Start and stop tunnel connections (ngrok, Cloudflare, custom) from the CLI. Inspect active tunnel URLs, configure authentication, and test external reachability.

## Quick install

```bash
npm install -g devilroute   # or: npx devilroute
devilroute --version
```

## Subcommands

### `tunnel`

**Example:**

```bash
devilroute tunnel
```

### `tunnel list`

**Flags:**

- `--json`

**Example:**

```bash
devilroute tunnel list
```

### `tunnel create [type]`

**Example:**

```bash
devilroute tunnel create [type]
```

### `tunnel stop <type>`

**Flags:**

- `--yes`

**Example:**

```bash
devilroute tunnel stop <type>
```

### `tunnel status <type>`

**Flags:**

- `--json`

**Example:**

```bash
devilroute tunnel status <type>
```

### `tunnel logs <type>`

**Flags:**

- `--tail <n>`

**Example:**

```bash
devilroute tunnel logs <type>
```

### `tunnel info <type>`

**Flags:**

- `--json`

**Example:**

```bash
devilroute tunnel info <type>
```

### `tunnel rotate <type>`

**Flags:**

- `--yes`

**Example:**

```bash
devilroute tunnel rotate <type>
```
