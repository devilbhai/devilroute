---
name: cli-batches
description: Submit and monitor batch inference jobs from the CLI. Upload and manage files for batch processing, retrieve results, and integrate batch pipelines with CI/CD workflows.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Submit and monitor batch inference jobs from the CLI. Upload and manage files for batch processing, retrieve results, and integrate batch pipelines with CI/CD workflows.

## Quick install

```bash
npm install -g devilroute   # or: npx devilroute
devilroute --version
```

## Subcommands

### `batches`

**Example:**

```bash
devilroute batches
```

### `batches list`

**Flags:**

- `--status <s>`
- `--limit <n>`

**Example:**

```bash
devilroute batches list
```

### `batches get <batchId>`

**Example:**

```bash
devilroute batches get <batchId>
```

### `batches create`

**Flags:**

- `--endpoint <e>`
- `--completion-window <w>`
- `--metadata <kv>`

**Example:**

```bash
devilroute batches create
```

### `batches submit`

**Flags:**

- `--endpoint <e>`
- `--wait`

**Example:**

```bash
devilroute batches submit
```

### `batches cancel <batchId>`

**Flags:**

- `--yes`

**Example:**

```bash
devilroute batches cancel <batchId>
```

### `batches wait <batchId>`

**Flags:**

- `--timeout <ms>`

**Example:**

```bash
devilroute batches wait <batchId>
```

### `batches output <batchId>`

**Flags:**

- `--out <path>`

**Example:**

```bash
devilroute batches output <batchId>
```

### `batches errors <batchId>`

**Flags:**

- `--out <path>`

**Example:**

```bash
devilroute batches errors <batchId>
```

### `files`

**Example:**

```bash
devilroute files
```

### `files list`

**Flags:**

- `--purpose <p>`
- `--limit <n>`

**Example:**

```bash
devilroute files list
```

### `files get <fileId>`

**Example:**

```bash
devilroute files get <fileId>
```

### `files upload <path>`

**Example:**

```bash
devilroute files upload <path>
```

### `files content <fileId>`

**Flags:**

- `--out <path>`

**Example:**

```bash
devilroute files content <fileId>
```

### `files delete <fileId>`

**Flags:**

- `--yes`

**Example:**

```bash
devilroute files delete <fileId>
```
