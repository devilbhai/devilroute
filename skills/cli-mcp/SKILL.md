---
name: cli-mcp
description: Inspect the MCP server status, list registered tools and scopes, run tool invocations, and manage MCP audit logs from the CLI.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect the MCP server status, list registered tools and scopes, run tool invocations, and manage MCP audit logs from the CLI.

## Quick install

```bash
npm install -g devilroute   # or: npx devilroute
devilroute --version
```

## Subcommands

### `mcp`

**Example:**

```bash
devilroute mcp
```

### `mcp status`

Show MCP server status

**Flags:**

- `--json`

**Example:**

```bash
devilroute mcp status
```

### `mcp restart`

Restart the MCP server

**Example:**

```bash
devilroute mcp restart
```

### `mcp call <tool> [argsJson]`

**Flags:**

- `--args <json>`
- `--args-file <path>`
- `--stream`
- `--scope <s>`

**Example:**

```bash
devilroute mcp call <tool> [argsJson]
```

### `mcp scopes`

**Flags:**

- `--tool <name>`

**Example:**

```bash
devilroute mcp scopes
```

### `mcp tools`

**Example:**

```bash
devilroute mcp tools
```

### `mcp list`

**Flags:**

- `--scope <s>`

**Example:**

```bash
devilroute mcp list
```

### `mcp info <name>`

**Example:**

```bash
devilroute mcp info <name>
```

### `mcp schema <name>`

**Flags:**

- `--io <kind>`

**Example:**

```bash
devilroute mcp schema <name>
```

### `mcp audit`

**Example:**

```bash
devilroute mcp audit
```

### `mcp tail`

**Flags:**

- `--follow`
- `--limit <n>`

**Example:**

```bash
devilroute mcp tail
```

### `mcp stats`

**Flags:**

- `--period <p>`

**Example:**

```bash
devilroute mcp stats
```
