---
name: cli-contexts
description: Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.

## Quick install

```bash
npm install -g devilroute   # or: npx devilroute
devilroute --version
```

## Subcommands

### `context-eng`

**Example:**

```bash
devilroute context-eng
```

### `context-eng analytics`

**Flags:**

- `--period <p>`

**Example:**

```bash
devilroute context-eng analytics
```

### `context-eng caveman`

**Example:**

```bash
devilroute context-eng caveman
```

### `context-eng config`

**Example:**

```bash
devilroute context-eng config
```

### `context-eng show`

**Example:**

```bash
devilroute context-eng show
```

### `context-eng set`

**Flags:**

- `--aggressiveness <n>`
- `--max-shrink-pct <n>`
- `--preserve-tags <list>`

**Example:**

```bash
devilroute context-eng set
```

### `context-eng rtk`

**Example:**

```bash
devilroute context-eng rtk
```

### `context-eng config`

**Example:**

```bash
devilroute context-eng config
```

### `context-eng show`

**Example:**

```bash
devilroute context-eng show
```

### `context-eng set`

**Flags:**

- `--token-budget <n>`
- `--reserve-pct <n>`

**Example:**

```bash
devilroute context-eng set
```

### `context-eng filters`

**Example:**

```bash
devilroute context-eng filters
```

### `context-eng list`

**Example:**

```bash
devilroute context-eng list
```

### `context-eng add`

**Flags:**

- `--priority <n>`
- `--action <a>`

**Example:**

```bash
devilroute context-eng add
```

### `context-eng remove <id>`

**Flags:**

- `--yes`

**Example:**

```bash
devilroute context-eng remove <id>
```

### `context-eng test`

**Example:**

```bash
devilroute context-eng test
```

### `context-eng raw-output <id>`

**Example:**

```bash
devilroute context-eng raw-output <id>
```

### `context-eng combos`

**Example:**

```bash
devilroute context-eng combos
```

### `context-eng list`

**Example:**

```bash
devilroute context-eng list
```

### `context-eng get <id>`

**Example:**

```bash
devilroute context-eng get <id>
```

### `context-eng assignments <id>`

**Example:**

```bash
devilroute context-eng assignments <id>
```

### `contexts`

**Example:**

```bash
devilroute contexts
```

### `contexts list`

List all contexts

**Example:**

```bash
devilroute contexts list
```

### `contexts add <name>`

Add a new context

**Flags:**

- `--api-key <k>`
- `--api-key-stdin`
- `--access-token <t>`
- `--access-token-stdin`
- `--scope <s>`
- `--description <d>`

**Example:**

```bash
devilroute contexts add <name>
```

### `contexts use <name>`

Switch active context

**Example:**

```bash
devilroute contexts use <name>
```

### `contexts current`

Show the active context (server, auth, scope)

**Flags:**

- `--name-only`

**Example:**

```bash
devilroute contexts current
```

### `contexts show <name>`

Show context details

**Example:**

```bash
devilroute contexts show <name>
```

### `contexts remove <name>`

Remove a context

**Flags:**

- `--yes`

**Example:**

```bash
devilroute contexts remove <name>
```

### `contexts rename <old> <new>`

Rename a context

**Example:**

```bash
devilroute contexts rename <old> <new>
```

### `contexts export`

Export contexts to JSON

**Flags:**

- `--out <path>`
- `--no-secrets`

**Example:**

```bash
devilroute contexts export
```

### `contexts import <file>`

Import contexts from a JSON file

**Flags:**

- `--merge`

**Example:**

```bash
devilroute contexts import <file>
```

### `sessions`

**Example:**

```bash
devilroute sessions
```

### `sessions list`

**Flags:**

- `--user <u>`
- `--kind <k>`
- `--active`
- `--limit <n>`

**Example:**

```bash
devilroute sessions list
```

### `sessions show <sessionId>`

**Example:**

```bash
devilroute sessions show <sessionId>
```

### `sessions expire <sessionId>`

**Flags:**

- `--yes`

**Example:**

```bash
devilroute sessions expire <sessionId>
```

### `sessions expire-all`

**Flags:**

- `--yes`

**Example:**

```bash
devilroute sessions expire-all
```

### `sessions current`

**Example:**

```bash
devilroute sessions current
```
